<div class="topics_main">
<div class="topic_comment">
<%= form_with(model: [@diary, @diary_comment], local: true) do |f|%>

            <div class="field">
              <%= f.label :diary_comment, "コメントを編集する" %><br />
              <%= f.text_field :diary_comment, id:"comment_content" %>
            </div>
            <div class="actions">
              <%= f.submit "編集", class: :form__btn  %>
            </div>
          <% end %>
           </div>
        </div>
